<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.MyAndroidAppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Remove or set to true to allow dark mode based on system/compose theme -->
        <item name="android:forceDarkAllowed" tools:targetApi="q">false</item>
        <!-- Removing explicit status bar color to let Compose handle it -->
        <!-- <item name="android:statusBarColor">#FFFFFF</item> -->
        <!-- Removing explicit light status bar to let Compose handle it -->
        <!-- <item name="android:windowLightStatusBar">true</item> -->
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

</resources>